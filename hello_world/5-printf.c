/**
 * main - function's name, prints a text
 *
 * this function prints a text using printf
 *
 * Return: Always 0
 */
#include <stdio.h>

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
