#include "main.h"
#include <stdio.h>

/**
 * _strlen - function that returns the length of a string.
 *
 * @s: string
 *
 * Return: 0
 */

int _strlen(char *s)
{
	size_t length;

	length = _strlen(*s);
}
